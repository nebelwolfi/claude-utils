{
  "name": "ralph",
  "version": "1.0.0",
  "description": "Parallel Claude worker orchestrator for kanban task processing",
  "type": "module",
  "main": "dist/index.js",
  "installConfig": {
    "displayName": "Ralph Worker Loop",
    "componentType": "cli"
  },
  "bin": {
    "Ralph-Loop": "dist/index.js",
    "Ralph-GUI": "dist/gui.js"
  },
  "scripts": {
    "build": "tsc && node -e \"require('fs').mkdirSync('dist/dashboard',{recursive:true});require('fs').copyFileSync('src/dashboard/app.html','dist/dashboard/app.html')\"",
    "prepare": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "kanban-mcp": "file:../kanban-mcp",
    "tree-kill": "^1.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
